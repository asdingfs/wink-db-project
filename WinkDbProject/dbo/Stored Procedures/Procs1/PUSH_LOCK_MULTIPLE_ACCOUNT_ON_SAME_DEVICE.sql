CREATE PROC PUSH_LOCK_MULTIPLE_ACCOUNT_ON_SAME_DEVICE
@device_token varchar(500),
@device_id varchar(10),
@customer_id varchar(50),
@active_status varchar(10)

AS

BEGIN
	
	SELECT @device_token
	SELECT @device_id
	SELECT @customer_id
	SELECT @active_status

END